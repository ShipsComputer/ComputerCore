<%= form_for(environmental_reading_type) do |f| %>
  <% if environmental_reading_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(environmental_reading_type.errors.count, "error") %> prohibited this environmental_reading_type from being saved:</h2>

      <ul>
      <% environmental_reading_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :handle %>
    <%= f.text_field :handle %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
